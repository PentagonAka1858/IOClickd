# Docker Environment Variables Example
# Copy this file to .env and configure as needed

# MySQL Configuration
DB_DATABASE=pfg_db
DB_USERNAME=pfg_user
DB_PASSWORD=pfg_password
DB_ROOT_PASSWORD=root

# Laravel Application
APP_ENV=production
APP_DEBUG=false
